import { createAction } from 'redux-actions';
export const loginInit = createAction('LOGIN_INIT');
export const setUserName = createAction('SET_LOGIN_USERNAME');
export const setPassword = createAction('SET_LOGIN_PASSWORD');
